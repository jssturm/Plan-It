"""Self-contained travel planning engine — zero API keys required.

Uses DuckDuckGo for web research and a deterministic rules engine
to build optimized itineraries. Runs entirely locally within
the installed environment.
"""
